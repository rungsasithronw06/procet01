# Decision Log: Thai Botanical Atlas

## ข้อมูลและผู้ใช้

งานนี้ใช้ CKAN API ของ data.go.th ซึ่งเป็นข้อมูลสวนพฤกษศาสตร์ไทย แบ่งตามภูมิภาค และมีข้อมูลจำนวนผู้เข้าชมปีงบประมาณ 2567 ผู้ใช้หลักคือคนที่กำลังหาสวนเพื่อไปเที่ยวหรือเปรียบเทียบสวนหลายแห่ง ผู้ใช้จึงต้องการค้นหาชื่อสวน ดูสวนที่ได้รับความนิยม เลือกภูมิภาค และดูว่าสวนอยู่ห่างจากสวนอ้างอิงเท่าไร

## (ก) เลือก DSA อะไร และช่วย UX อย่างไร

### 1. Merge Sort

ใช้ Merge Sort สำหรับเรียงสวนตามยอดผู้เข้าชมจากมากไปน้อย และเรียงตามระยะทางจากใกล้ไปไกล เมื่อผู้ใช้กด dropdown หรือปุ่ม ระบบจะเรียงข้อมูลที่มีอยู่ในหน้าเว็บทันที โดยไม่ต้องส่งคำขอใหม่ไปที่ API

### 2. Hash Map

ใช้ Hash Map เก็บจำนวนผู้เข้าชมโดยใช้ชื่อสวนที่ผ่านการปรับรูปแบบแล้วเป็นกุญแจ ชื่อจากข้อมูลสวนกับชื่อจากข้อมูลผู้เข้าชมอาจมีช่องว่างหรือคำว่า "สวนพฤกษศาสตร์" ไม่เหมือนกัน จึงตัดคำและช่องว่างออกก่อนจับคู่ ทำให้หน้าเว็บแสดงยอดผู้เข้าชมได้ถูกต้องและค้นข้อมูลชื่อสวนได้เร็วขึ้น

### 3. Linear Search

ใช้ Linear Search สำหรับช่องค้นหาและคำแนะนำชื่อสวน ระบบเดินดูชื่อสวนทีละรายการและเทียบตัวอักษรทีละตำแหน่ง วิธีนี้ตรงกับพฤติกรรมของผู้ใช้ที่พิมพ์คำค้นและต้องการเห็นผลลัพธ์ทันที

### 4. Haversine

ใช้สูตร Haversine คำนวณระยะทางจากพิกัดของสวนอ้างอิงไปยังสวนอื่น แล้วนำผลลัพธ์ไปเรียงด้วย Merge Sort ผู้ใช้จึงเลือกสวนอ้างอิงและเห็นสวนที่อยู่ใกล้ที่สุดก่อน

## (ข) ถ้าไม่ใช้ DSA จะเสีย UX อะไร

ถ้าไม่ใช้ Merge Sort ผู้ใช้จะไม่สามารถเรียงสวนตามยอดผู้เข้าชมหรือระยะทางได้ทันที และอาจต้องรอให้ backend เรียงข้อมูลให้ทุกครั้งที่เปลี่ยนตัวเลือก

ถ้าไม่ใช้ Hash Map การจับคู่ชื่อสวนกับยอดผู้เข้าชมจะยุ่งยากขึ้น และอาจต้องเดินเทียบข้อมูลหลายรอบ ทำให้ตอบสนองช้าลงเมื่อข้อมูลมีจำนวนมาก

ถ้าไม่ใช้ Linear Search ระบบค้นหาชื่อและคำแนะนำระหว่างพิมพ์จะไม่มี logic ที่ตรงกับการเดินดูข้อมูลทีละรายการ ผู้ใช้จะไม่เห็นผลลัพธ์ที่ค้นหาได้ทันที

ถ้าไม่ใช้ Haversine ผู้ใช้จะไม่รู้ว่าสวนใดอยู่ใกล้สวนอ้างอิง และฟังก์ชันค้นหาตามระยะทางจะไม่เกิดประโยชน์จริง

## (ค) เขียนเองหรือใช้ built-in เพราะอะไร

ส่วน Sort, Search, การคัดลอกข้อมูล และการหยิบข้อมูลบางส่วนเขียนเองด้วย loop และการเปรียบเทียบของเราเอง เพราะเป็นข้อกำหนดของวิชาและต้องการแสดงให้เห็นขั้นตอนของ DSA อย่างชัดเจน จึงไม่ใช้ `Array.sort()`, `Array.indexOf()` หรือ `Array.slice()` ในส่วน logic เหล่านี้

ส่วนที่ไม่ใช่หัวใจของ DSA เช่น `fetch()` สำหรับเรียก API, `replace()` สำหรับปรับข้อความ, `JSON.parse()` สำหรับอ่าน JSON และ DOM API สำหรับวาดหน้าเว็บ ยังคงใช้คำสั่งมาตรฐาน เพราะเป็นงานรับส่งข้อมูล แปลงข้อความ และแสดงผล ไม่ใช่ logic ของ DSA ที่โจทย์กำหนดให้เขียนเอง

## การเลือก Express

API ฝั่ง server ใช้ Node.js และ Express เป็นตัวรับคำขอจากหน้าเว็บที่ `/api/proxy` จากนั้น Express จะเรียก data.go.th, ใช้ cache เมื่อมีข้อมูลเดิม และใช้ backup.json เมื่อ API หลักไม่ตอบกลับ หน้าเว็บจึงเรียก endpoint ของเราเองและไม่ต้องจัดการปัญหา CORS โดยตรง
